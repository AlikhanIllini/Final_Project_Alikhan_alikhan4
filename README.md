# Stock Cards — Ultimate Edition
The grown-up version of Stock Cards Lite. Adds multi-user auth, tags & saved filters,
a free-tier price adapter (with caching + manual fallback), weekly email digest,
and simple history on each card—while keeping the same prioritization workflow.